# AETHON Frontend

Frontend dashboard and governance UI will live here, separate from the backend.

The backend exposes REST (`/v1/*`) and WebSocket endpoints consumed by this layer.

```bash
# Coming soon — connect to backend at http://localhost:3001
```
