[build]
builder = "NIXPACKS"
buildCommand = "npm run build"

[deploy]
startCommand = "node dist/railway-start.cjs"
healthcheckPath = "/v1/health/live"
healthcheckTimeout = 300
restartPolicyType = "ON_FAILURE"
