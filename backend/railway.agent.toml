# Agent worker services — set AETHON_RUNTIME=agent on Railway (see docs/RAILWAY_AGENTS.md).
# Railway uses backend/railway.toml; routing is via scripts/railway-start.cjs + AETHON_RUNTIME.

AETHON_RUNTIME=agent
