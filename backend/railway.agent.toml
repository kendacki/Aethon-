# Agent worker services — set AETHON_RUNTIME=agent on Railway (see docs/RAILWAY_AGENTS.md).
# db/migrate.js exits immediately when AETHON_RUNTIME=agent (no Postgres on workers).

[deploy]
preDeployCommand = "npm run db:migrate:prod"
startCommand = "node dist/railway-start.cjs"
